import Placeholder from '../components/Placeholder';

export default function ArticlesScreen() {
  return (
    <Placeholder
      endpoint="GET /articles"
      note="靈修佳文列表與內容頁，支援分類（每日靈糧／牧者專欄）。"
    />
  );
}
